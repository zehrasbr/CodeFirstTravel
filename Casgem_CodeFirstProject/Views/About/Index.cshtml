@{
    Layout = null;
}

<!doctype html>
<html lang="en">
@Html.Action("PartialHead", "Default")
<body id="home">
    @Html.Action("PartialMainPage")
    @Html.Action("PartialNavbar", "Default")
    @Html.Action("PartialScript")
    @Html.Action("PartialDetail", "Contact")
    @Html.Action("PartialGuide")
    <section class="w3l-footer-29-main">
        <div class="footer-29 py-5 ">
            <div class="container">
                <div class="grid-col-4 footer-top-29">
                    <div class="footer-list-29 footer-1">
                    </div>

                    @Html.Action("PartialCategory", "Default")
                    @Html.Action("PartialGalery", "Default")
                    @Html.Action("PartialTravelTrip", "Default")
                </div>
            </div>
        </div>
    </section>
    @Html.Action("Team","Default")
    @Html.Action("PartialFooter","Default")
    @Html.Action("PartialScript", "Default")

</body>

</html>

